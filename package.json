{
  "name": "line-bot-router-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Workers で動く config-driven な LINE Bot Router",
  "type": "module",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "echo \"(lint is not configured yet)\"",
    "test": "pnpm --filter line-bot-router test",
    "dev:worker": "pnpm --filter @templates/cloudflare-worker dev"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
